
const API_URL = 'https://swapi.dev/api/';

const API_SHIPS = `${API_URL}starships`;

const API_PEOPLE = `${API_URL}people`;

const API_PLANETS = `${API_URL}planets`;

export {
    API_URL,
    API_SHIPS,
    API_PEOPLE,
    API_PLANETS
}